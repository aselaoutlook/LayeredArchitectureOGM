<div class="box box-info">
    <div class="box-header with-border">
        <h3 class="box-title">StrategicDriver List</h3>
    </div>
    <div id="gridContent" class="box-body">
        <table id="tblStrategicDrivers" class="table table-bordered table-striped" ng-table="strategicDriverVm.tableParams">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Is Active</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="strategicDriver in $data">
                    <td data-title="'Name'">{{strategicDriver.Name}}</td>
                    <td data-title="'Description'">{{strategicDriver.Description}}</td>
                    <td data-title="'Is Active'">{{strategicDriver.IsActive}}</td>
                    <td data-title="'Edit'">
                        <button type="button"
                                class="btn btn-default btn-xs pull-right"
                                ng-click="LoadById(strategicDriver.StrategicDriverId)">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
            </tbody>
            <tfoot></tfoot>
        </table>
        @Resources.SystemResources.Common_webgrid_row_count_text <span id="rowCount">{{strategicDriverVm.tableParams.total()}}</span>
    </div>
</div>