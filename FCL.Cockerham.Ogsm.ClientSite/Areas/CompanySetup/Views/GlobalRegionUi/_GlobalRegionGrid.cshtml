<div class="box box-info">
    <div class="box-header with-border">
        <h3 class="box-title">GlobalRegion List</h3>
    </div>
    <div id="gridContent" class="box-body">
        <table id="tblGlobalRegions" class="table table-bordered table-striped" ng-table="globalRegionVm.tableParams">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="globalRegion in $data">
                    <td data-title="'Name'">{{globalRegion.Name}}</td>
                    <td data-title="'Edit'">
                        <button type="button"
                                class="btn btn-default btn-xs pull-right"
                                ng-click="LoadById(globalRegion.GlobalRegionId)">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span>
                        </button>
                    </td>
                </tr>
            </tbody>
            <tfoot></tfoot>
        </table>
        @Resources.SystemResources.Common_webgrid_row_count_text <span id="rowCount">{{globalRegionVm.tableParams.total()}}</span>
    </div>
</div>